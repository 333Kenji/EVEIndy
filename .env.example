# Core application settings
APP_ENV=development
LOG_LEVEL=INFO
API_ROOT_URL=http://localhost:8000

# Database connectivity
DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@localhost:5432/eveindy

# Redis cache / queue
REDIS_URL=redis://localhost:6379/0

# ESI OAuth credentials (do not commit real values)
ESI_CLIENT_ID=
ESI_CLIENT_SECRET=
ESI_REFRESH_TOKEN=
ESI_CALLBACK_URL=http://localhost:8000/auth/esi/callback

# External market data endpoints
ADAM4EVE_BASE_URL=https://api.adam4eve.eu
FUZZWORK_BASE_URL=https://market.fuzzwork.co.uk

# Alerting / integrations
DISCORD_WEBHOOK_URL=

# Feature flags
ENABLE_DISCORD_ALERTS=false

# Price backfill/refresh settings
PRICE_PROVIDER=adam4eve
REGION_ID=10000002
# Jita materials for Nitrogen Blocks + common minerals
PRICE_TYPE_IDS=16272,16273,44,3689,9832,34,35,36,37

# Provider rate limit settings (token bucket)
ESI_CAPACITY=10.0
ESI_REFILL_RATE=2.0
ADAM4EVE_CAPACITY=1.0
ADAM4EVE_REFILL_RATE=0.1
FUZZWORK_CAPACITY=1.0
FUZZWORK_REFILL_RATE=0.2
